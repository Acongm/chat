{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'ERROR: Use the chat-web Vercel project (Root Directory = apps/web). Disconnect this root-level chat project.' && exit 1"
}
